# Lightweight requirements for CI - excludes heavy ML dependencies
# This file is used in CI to avoid "No space left on device" errors

# Core Dependencies
fastapi>=0.121.2
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pydantic[email]>=2.5.0  # Includes email_validator
python-dotenv>=1.0.0
click>=8.1.0
email-validator>=2.0.0  # Email validation for Pydantic

# Database Adapters
asyncpg>=0.29.0
aiomysql>=0.2.0
motor>=3.3.2
cassandra-driver>=3.29.0
elasticsearch>=8.11.0
sqlalchemy>=2.0.23

# Vector Database (client only, no heavy ML dependencies)
qdrant-client>=1.7.0
# NOTE: sentence-transformers and tf-keras excluded for CI
# These are large packages (TensorFlow/PyTorch) that cause disk space issues
# The EmbeddingGenerator handles missing dependencies gracefully

# LLM - Updated for compatibility
openai>=1.6.1,<3.0.0
langchain>=0.0.350
langchain-openai>=0.0.2

# Caching
redis>=5.0.1
cachetools>=5.3.2

# Visualization (lightweight subset)
# NOTE: plotly and kaleido excluded - they're large and not needed for basic tests
pandas>=2.1.4
numpy>=1.26.2
openpyxl>=3.1.2  # Excel export support
pyarrow>=14.0.1  # Parquet export support
fastavro>=1.8.2  # Avro export support

# Utilities
httpx>=0.25.2
aiohttp>=3.9.1
pyyaml>=6.0.1
aiosqlite>=0.19.0
pyjwt>=2.8.0  # JWT authentication
prometheus-client>=0.19.0  # Prometheus metrics

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Development
black>=23.11.0
flake8>=6.1.0
mypy>=1.7.1
isort>=5.12.0

